'use client';
import {useMemo,useState} from 'react';
export function FindReplace(){const [t,setT]=useState('사과와 사과 주스'),[find,setFind]=useState('사과'),[rep,setRep]=useState('오렌지');const out=useMemo(()=>find?t.split(find).join(rep):t,[t,find,rep]);return <div className="workspace"><textarea className="toolTextarea" value={t} onChange={e=>setT(e.target.value)}/><div className="jsonEditorGrid"><input className="toolInput" value={find} onChange={e=>setFind(e.target.value)} placeholder="찾을 내용"/><input className="toolInput" value={rep} onChange={e=>setRep(e.target.value)} placeholder="바꿀 내용"/></div><textarea className="toolTextarea" readOnly value={out}/></div>}
export function NumberExtractor(){const [t,setT]=useState('주문번호 12345, 금액 29,900원');const out=useMemo(()=>t.match(/[-+]?\d[\d,.]*/g)?.join('\n')||'',[t]);return <div className="workspace"><textarea className="toolTextarea" value={t} onChange={e=>setT(e.target.value)}/><textarea className="toolTextarea" readOnly value={out} placeholder="추출된 숫자"/></div>}
export function LinkExtractor(){const [t,setT]=useState('사이트 https://example.com 과 https://openai.com 을 확인하세요.');const out=useMemo(()=>[...new Set(t.match(/https?:\/\/[^\s<>"']+/g)||[])].join('\n'),[t]);return <div className="workspace"><textarea className="toolTextarea" value={t} onChange={e=>setT(e.target.value)}/><textarea className="toolTextarea" readOnly value={out} placeholder="추출된 URL"/></div>}
export function EmailExtractor(){const [t,setT]=useState('문의: hello@example.com');const out=useMemo(()=>[...new Set(t.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/gi)||[])].join('\n'),[t]);return <div className="workspace"><textarea className="toolTextarea" value={t} onChange={e=>setT(e.target.value)}/><textarea className="toolTextarea" readOnly value={out} placeholder="추출된 이메일"/></div>}
