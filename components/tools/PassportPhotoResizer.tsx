'use client';
import {useState} from 'react';
const presets=[['증명사진 3×4cm',354,472],['여권사진 3.5×4.5cm',413,531],['반명함 3×4cm',354,472]] as const;
export default function PassportPhotoResizer(){const [file,setFile]=useState<File|null>(null);const [p,setP]=useState(1);const save=async()=>{if(!file)return;const im=new Image();im.src=URL.createObjectURL(file);await im.decode();const [,w,h]=presets[p];const c=document.createElement('canvas');c.width=w;c.height=h;const g=c.getContext('2d')!;g.fillStyle='#fff';g.fillRect(0,0,w,h);const s=Math.max(w/im.naturalWidth,h/im.naturalHeight),dw=im.naturalWidth*s,dh=im.naturalHeight*s;g.drawImage(im,(w-dw)/2,(h-dh)/2,dw,dh);c.toBlob(b=>{if(!b)return;const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='id-photo.jpg';a.click()},'image/jpeg',.94)};return <div className="workspace"><input type="file" accept="image/*" onChange={e=>setFile(e.target.files?.[0]||null)}/><label className="settingField"><span>사진 규격</span><select className="toolInput" value={p} onChange={e=>setP(Number(e.target.value))}>{presets.map((x,i)=><option value={i} key={x[0]}>{x[0]} ({x[1]}×{x[2]}px)</option>)}</select></label><button className="primaryButton" disabled={!file} onClick={save}>규격에 맞춰 저장</button><p className="settingHint">300dpi 기준 픽셀 크기로 중앙 채움 처리합니다. 실제 제출처의 최신 사진 규정을 반드시 확인하세요.</p></div>}
